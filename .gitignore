# ECC harness is a local dev aid, not a build artifact (Windows AV locks on commit)
.claude/

node_modules/
.env
*.log
dist/
results/
.tsbuildinfo

# Personal print-outs (markdown versions are tracked)
docs/*.pdf
